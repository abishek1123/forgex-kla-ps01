# Minimal set to run the EVALUATION SCRIPT only.
#
#   pip install -r requirements-inference.txt
#   python run.py <input-dir> <output-dir>
#
# run.py inlines the network definition and imports nothing else, so these two
# packages are genuinely all it needs. This file is a convenience, not the
# environment specification -- that is requirements.txt, the full pip freeze.
--extra-index-url https://download.pytorch.org/whl/cu126

torch==2.13.0+cu126
numpy==2.4.4

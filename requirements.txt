# Complete `pip freeze` output of the environment that trained models/model.pt.
# Python 3.13, Windows 11, NVIDIA RTX 4050 Laptop, CUDA 12.6.
#
# One line has been ADDED and none altered or removed: torch and torchvision are
# CUDA 12.6 builds that do not exist on PyPI, so without the index URL below
# `pip install -r requirements.txt` either fails outright or silently installs a
# CPU build. Every package line under it is the freeze output verbatim.
--extra-index-url https://download.pytorch.org/whl/cu126

colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
filelock==3.29.0
fonttools==4.63.0
fsspec==2026.4.0
Jinja2==3.1.6
kiwisolver==1.5.0
lpips==0.1.4
MarkupSafe==3.0.3
matplotlib==3.11.1
mpmath==1.3.0
networkx==3.6.1
numpy==2.4.4
packaging==26.3
pillow==12.2.0
pip==26.0.1
pyparsing==3.3.2
python-dateutil==2.9.0.post0
scipy==1.18.0
setuptools==78.1.0
six==1.17.0
sympy==1.14.0
torch==2.13.0+cu126
torchvision==0.28.0+cu126
tqdm==4.70.0
typing_extensions==4.15.0

# NOTE: run.py imports only torch and numpy. Everything else above is training
# and analysis. If you want the minimal set to run the evaluation script alone,
# see requirements-inference.txt -- but this file is the environment spec.
